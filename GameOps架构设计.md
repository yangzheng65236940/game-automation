# GameOps — 游戏服务器自动化运维平台架构设计

## 1. 项目概述

GameOps 是一套面向游戏运维团队的统一运维平台，覆盖游戏服务器的**起服、停服、合区、监控、日志、远程终端**六大核心能力，实现从日常操作到应急响应的全流程自动化。

---

## 2. 系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层 (Web UI)                          │
│  Dashboard │ 服务器管理 │ 合区中心 │ 监控告警 │ 日志中心 │ 终端  │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTPS / WebSocket
┌──────────────────────────▼──────────────────────────────────────┐
│                      API 网关 (Nginx / Kong)                    │
│              鉴权 · 限流 · 路由 · WebSocket 代理                 │
└──────┬──────────┬──────────┬──────────┬──────────┬─────────────┘
       │          │          │          │          │
┌──────▼───┐┌─────▼────┐┌───▼────┐┌────▼───┐┌────▼────┐
│ 用户服务  ││ 服务器    ││ 合区   ││ 监控   ││ 日志    │
│ Auth     ││ 管理      ││ 服务   ││ 服务   ││ 服务    │
│ RBAC     ││ 起停/终端 ││        ││        ││         │
└──────┬───┘└─────┬────┘└───┬────┘└────┬───┘└────┬────┘
       │          │          │          │          │
┌──────▼──────────▼──────────▼──────────▼──────────▼─────────────┐
│                        消息总线 (NATS / Redis Streams)           │
│              任务分发 · 事件广播 · 异步回调                       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                     Agent 集群 (每台游戏服务器)                   │
│  进程管理 │ 脚本执行 │ 日志采集 │ 指标上报 │ 终端代理 │ 文件传输  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                        基础设施层                                │
│  MySQL/PostgreSQL │ Redis │ MinIO/OSS │ Elasticsearch │ Prometheus │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 核心模块设计

### 3.1 服务器管理服务 (Server Manager)

负责游戏服务器的生命周期管理。

| 功能 | 说明 |
|------|------|
| 起服 | 按模板创建区服，分配资源，拉起游戏进程 |
| 停服 | 优雅停服（通知玩家→踢人→存档→停进程）与强制停服 |
| 重启 | 滚动重启、维护重启 |
| 状态查询 | 实时获取服务器运行状态、进程信息、资源占用 |

**起服流程：**

```
用户发起起服 → 参数校验 → 资源预检(端口/磁盘/内存)
    → 生成配置(区服ID/DB/Redis) → 下发任务到Agent
    → Agent拉起进程 → 健康检查 → 注册到路由/网关
    → 回调通知 → 更新DB状态 → 完成
```

**停服流程：**

```
用户发起停服 → 广播维护公告 → 等待玩家退出(超时强制踢出)
    → 触发存档 → 下发停服命令到Agent
    → Agent停止进程 → 确认进程退出 → 注销路由
    → 回调通知 → 更新DB状态 → 完成
```

### 3.2 合区服务 (Merge Service)

将多个区服数据合并为一个区服，是游戏运维中最复杂、风险最高的操作。

**合区流程：**

```
合区申请 → 合区方案审核 → 数据预检(角色冲突/帮派冲突/排名冲突)
    → 生成合区脚本 → 停止源区服和目标区服
    → 全量备份 → 执行数据迁移与冲突解决
    → 数据校验(角色数/资产/排行榜) → 启动目标区服
    → 玩家验证 → 合区完成/回滚
```

**冲突解决策略：**

| 冲突类型 | 解决策略 |
|----------|----------|
| 角色名重复 | 自动加后缀 + 提供免费改名卡 |
| 帮派名重复 | 保留活跃度高的，另一个加后缀 |
| 排行榜 | 按战力/等级重新排序合并 |
| 唯一ID冲突 | ID段重映射，全局唯一ID生成 |

### 3.3 监控服务 (Monitor Service)

全方位监控游戏服务器运行状态。

**监控维度：**

```
┌─────────────────────────────────────────┐
│              告警规则引擎                │
│   阈值告警 │ 趋势告警 │ 智能告警(ML)    │
└──────────┬──────────────────────────────┘
           │
┌──────────▼──────────────────────────────┐
│           指标采集层                     │
│                                         │
│  系统指标          应用指标          游戏业务指标    │
│  ├─ CPU/内存/磁盘   ├─ 场景服指标     ├─ 在线人数      │
│  ├─ 网络I/O        ├─ 登录服指标     ├─ 登录/注册数    │
│  ├─ 磁盘I/O        ├─ 社交服指标     ├─ 充值金额      │
│  └─ 进程状态        ├─ 聊天服指标     ├─ 帧率/延迟     │
│                     ├─ 公会服指标     ├─ 关键业务埋点   │
│                     ├─ 跨服战指标     └─ LTV/ARPU     │
│                     └─ 通用RPC指标                    │
└──────────┬──────────────────────────────┘
           │
┌──────────▼──────────────────────────────┐
│          Prometheus + Grafana            │
│    Agent 主动推送 / Prometheus 拉取      │
└─────────────────────────────────────────┘
```

**应用指标详情：**

| 服务 | 关键指标 | 说明 |
|------|----------|------|
| 场景服 | 场景在线人数、AOI更新频率、帧率、Tick耗时、实体数量 | 核心游戏逻辑服务，需重点监控帧率和Tick延迟 |
| 登录服 | 登录QPS、登录成功率、排队人数、Token验证延迟、会话数 | 登录入口，需关注排队和认证性能 |
| 社交服 | 好友操作QPS、邮件队列长度、组队匹配耗时、社交事件延迟 | 社交关系服务，需关注匹配和事件处理延迟 |
| 聊天服 | 消息QPS、频道消息堆积数、消息投递延迟、在线频道数 | 聊天吞吐量敏感，需监控消息堆积 |
| 公会服 | 公会操作QPS、公会战状态、公会数据加载耗时 | 公会相关业务，需关注公会战期间负载 |
| 跨服战 | 匹配耗时、战斗实例数、同步延迟、结算耗时 | 跨服活动期间流量尖峰，需重点监控 |
| 通用RPC | 请求QPS、P50/P95/P99延迟、错误率、超时率、活跃连接数 | 所有服务间调用的通用指标，用于服务治理和链路追踪 |

**告警通道：** 飞书/钉钉/企业微信/短信/邮件

### 3.4 日志服务 (Log Service)

集中式日志采集、存储、检索与分析。

```
游戏进程 → 本地日志文件 → Agent(Filebeat) → Kafka/Redis
    → Logstash/Filters → Elasticsearch → Kibana/Web UI
```

**功能清单：**

| 功能 | 说明 |
|------|------|
| 实时日志 | WebSocket 推送，类似 tail -f |
| 日志检索 | 全文检索，支持 Lucene 语法 |
| 日志过滤 | 按级别/模块/时间/关键词过滤 |
| 日志下载 | 按时间范围打包下载 |
| 日志归档 | 冷热分离，热数据 ES，冷数据对象存储 |
| 异常标记 | 自动识别 ERROR/CRASH 日志并高亮 |

### 3.5 远程终端服务 (Terminal Service)

通过 Web 浏览器直接连接服务器终端。

```
Web UI (xterm.js)
    │ WebSocket
    ▼
API 网关 (WebSocket 代理)
    │
    ▼
终端服务 (Go)
    │ SSH / WebSocket
    ▼
Agent (每台服务器)
    │ PTY
    ▼
服务器 Shell
```

**安全设计：**

- 所有终端操作录屏审计，存储到对象存储
- 危险命令拦截（`rm -rf /`、`drop database` 等）
- 会话超时自动断开
- 操作审批流（高危命令需二次确认）
- RBAC 权限控制，按项目/服务器授权

### 3.6 用户与权限服务 (Auth Service)

| 功能 | 说明 |
|------|------|
| 认证 | 账号密码 / LDAP / SSO / OAuth2 |
| 授权 | RBAC 角色模型 |
| 审计 | 所有操作记录审计日志 |
| 项目隔离 | 多项目/多游戏隔离 |

**角色模型：**

```
超级管理员 ─── 项目管理员 ─── 运维工程师
                                │
                            运维实习生（只读 + 需审批操作）
```

---

## 4. Agent 设计

Agent 部署在每台游戏服务器上，是平台的执行单元。

```
┌─────────────────────────────────────────┐
│              Agent (Go 单二进制)         │
│                                         │
│  ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 进程管理  │ │ 脚本引擎 │ │ 终端代理 │ │
│  │ Supervisor│ │ Lua/Shell│ │ PTY     │ │
│  └──────────┘ └──────────┘ └─────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 日志采集  │ │ 指标采集 │ │ 文件传输 │ │
│  │ Tail+Push│ │ Prometheus│ │ SFTP    │ │
│  └──────────┘ └──────────┘ └─────────┘ │
│                                         │
│  通信：WebSocket 长连接 + 心跳保活       │
│  安全：TLS 双向认证 + Token 鉴权         │
│  更新：热更新 / 自升级                   │
└─────────────────────────────────────────┘
```

**Agent 核心能力：**

| 能力 | 实现方式 |
|------|----------|
| 进程管理 | 管理 game-server 进程组，支持 start/stop/restart/status |
| 脚本执行 | 接收并执行运维脚本，返回执行结果 |
| 日志采集 | 实时 tail 日志文件，推送到消息总线 |
| 指标上报 | 采集系统+业务指标，推送到 Prometheus Pushgateway |
| 终端代理 | 创建 PTY，代理 WebSocket ↔ PTY 数据流 |
| 文件传输 | 上传/下载配置文件、补丁包、日志包 |

---

## 5. 数据库设计 (核心表)

### 服务器表 (servers)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| project_id | BIGINT | 所属项目 |
| name | VARCHAR | 服务器名称 |
| ip | VARCHAR | 服务器IP |
| ssh_port | INT | SSH端口 |
| agent_port | INT | Agent端口 |
| status | ENUM | running/stopped/maintaining/merging |
| zone_id | VARCHAR | 区服ID |
| tags | JSON | 标签(版本/类型/分组) |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 合区任务表 (merge_tasks)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| project_id | BIGINT | 所属项目 |
| source_zone_ids | JSON | 源区服ID列表 |
| target_zone_id | VARCHAR | 目标区服ID |
| status | ENUM | pending/approving/running/verifying/done/failed/rollback |
| merge_config | JSON | 合区配置(冲突策略/脚本等) |
| operator_id | BIGINT | 操作人 |
| started_at | TIMESTAMP | 开始时间 |
| finished_at | TIMESTAMP | 结束时间 |

### 操作审计表 (audit_logs)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 操作人 |
| project_id | BIGINT | 项目 |
| server_id | BIGINT | 目标服务器 |
| action | VARCHAR | 操作类型 |
| params | JSON | 操作参数 |
| result | VARCHAR | 操作结果 |
| ip | VARCHAR | 来源IP |
| created_at | TIMESTAMP | 操作时间 |

---

## 6. 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | React + TypeScript + Ant Design | 管理后台 UI |
| 终端前端 | xterm.js | Web 终端模拟器 |
| 后端 API | Go (Gin/Fiber) | 高并发、低资源占用 |
| 消息总线 | NATS / Redis Streams | 任务分发与事件广播 |
| Agent | Go | 单二进制，无依赖，跨平台 |
| 关系数据库 | PostgreSQL | 业务数据存储 |
| 缓存 | Redis | 会话/锁/缓存/消息队列 |
| 时序数据库 | Prometheus + VictoriaMetrics | 监控指标存储 |
| 日志存储 | Elasticsearch | 日志索引与检索 |
| 对象存储 | MinIO / S3 | 日志归档/终端录屏/备份 |
| 容器化 | Docker + K8s (可选) | 平台自身部署 |
| 反向代理 | Nginx | API网关/负载均衡/WS代理 |

---

## 7. 部署架构

```
                    ┌─────────────┐
                    │   负载均衡   │
                    │  (SLB/Nginx) │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
        ┌─────▼────┐ ┌────▼─────┐ ┌───▼──────┐
        │  Web-01  │ │  Web-02  │ │  Web-03  │
        │ (API服务) │ │ (API服务) │ │ (API服务) │
        └─────┬────┘ └────┬─────┘ └───┬──────┘
              │            │            │
        ┌─────▼────────────▼────────────▼──────┐
        │          Redis Cluster                │
        │      (缓存/会话/消息/分布式锁)         │
        └─────┬────────────────────────────────┘
              │
        ┌─────▼────────────────────────────────┐
        │          PostgreSQL (主从)             │
        └─────┬────────────────────────────────┘
              │
        ┌─────▼────────────────────────────────┐
        │    Elasticsearch + Prometheus         │
        │         (日志 + 监控)                  │
        └──────────────────────────────────────┘

        ┌──────────────────────────────────────┐
        │          游戏服务器集群                │
        │                                      │
        │  ┌──────┐  ┌──────┐  ┌──────┐       │
        │  │Game01│  │Game02│  │Game03│  ...   │
        │  │Agent │  │Agent │  │Agent │       │
        │  └──────┘  └──────┘  └──────┘       │
        └──────────────────────────────────────┘
```

---

## 8. 安全设计

| 层面 | 措施 |
|------|------|
| 传输安全 | 全链路 TLS，WebSocket WSS |
| 认证 | JWT Token + Refresh Token，支持 MFA |
| 授权 | RBAC + 项目级隔离，最小权限原则 |
| Agent 认证 | 双向 TLS + 注册 Token，防伪造 |
| 终端安全 | 危险命令拦截，操作录屏审计，会话水印 |
| 数据安全 | 敏感配置加密存储，数据库字段加密 |
| 备份恢复 | 合区前全量备份，支持一键回滚 |
| 网络隔离 | Agent 仅出站连接，无需开放入站端口 |

---

## 9. 高可用设计

| 组件 | 高可用方案 |
|------|-----------|
| API 服务 | 无状态，多实例 + 负载均衡，健康检查自动摘除 |
| PostgreSQL | 主从复制 + 自动故障转移 (Patroni) |
| Redis | Sentinel / Cluster 模式 |
| Elasticsearch | 3 节点以上集群，分片+副本 |
| Prometheus | VictoriaMetrics 集群模式 / Thanos 长期存储 |
| Agent | 断线自动重连，任务本地持久化，重连后续传 |
| 消息总线 | NATS Cluster / Redis Stream 消费者组 |

---

## 10. 项目目录结构

```
gameops/
├── cmd/
│   ├── server/          # API 服务入口
│   └── agent/           # Agent 入口
├── internal/
│   ├── auth/            # 认证授权
│   ├── servermgr/       # 服务器管理
│   ├── merge/           # 合区服务
│   ├── monitor/         # 监控服务
│   ├── logsvc/          # 日志服务
│   ├── terminal/        # 终端服务
│   └── agent/           # Agent 核心逻辑
├── pkg/
│   ├── ws/              # WebSocket 封装
│   ├── ssh/             # SSH 客户端
│   ├── pty/             # PTY 管理
│   └── protocol/        # 通信协议定义
├── web/                 # 前端项目
│   ├── src/
│   │   ├── pages/
│   │   │   ├── dashboard/
│   │   │   ├── servers/
│   │   │   ├── merge/
│   │   │   ├── monitor/
│   │   │   ├── logs/
│   │   │   └── terminal/
│   │   └── components/
│   └── package.json
├── deploy/              # 部署配置
│   ├── docker/
│   ├── k8s/
│   └── scripts/
├── docs/
└── configs/
```

---

## 11. 开发路线图

### Phase 1 — 基础框架 (4周)
- [ ] 项目脚手架搭建 (Go + React)
- [ ] 用户认证与 RBAC
- [ ] Agent 开发与部署
- [ ] 服务器 CRUD 与 Agent 注册

### Phase 2 — 核心功能 (6周)
- [ ] 起服/停服/重启流程
- [ ] 远程终端 (xterm.js + PTY)
- [ ] 日志采集与实时查看
- [ ] 基础监控 (系统指标 + Grafana)

### Phase 3 — 高级功能 (6周)
- [ ] 合区流程与冲突解决
- [ ] 告警规则引擎与通知
- [ ] 终端审计与危险命令拦截
- [ ] 批量操作与运维编排

### Phase 4 — 生产加固 (4周)
- [ ] 高可用部署方案
- [ ] 性能调优与压测
- [ ] 安全审计与渗透测试
- [ ] 灰度发布与回滚机制

---

## 12. 关键接口定义

### 起服

```
POST /api/v1/servers/{id}/start
Request:  { "force": false, "params": { "zone_id": "s1" } }
Response: { "task_id": "xxx", "status": "running" }
```

### 停服

```
POST /api/v1/servers/{id}/stop
Request:  { "graceful": true, "timeout": 300, "announce": "维护公告内容" }
Response: { "task_id": "xxx", "status": "running" }
```

### 合区

```
POST /api/v1/merges
Request:  {
  "source_zone_ids": ["s1", "s2"],
  "target_zone_id": "s1",
  "conflict_policy": { "name": "suffix", "id": "remap" }
}
Response: { "task_id": "xxx", "status": "pending" }
```

### 终端连接

```
WebSocket /api/v1/servers/{id}/terminal?token=xxx
双向流: stdin/stdout/stderr + resize 信号
```

### 日志流

```
WebSocket /api/v1/servers/{id}/logs/stream?path=/var/log/game/server.log&follow=true
服务端推送日志行，客户端可动态过滤
```

---

*文档版本: v1.0 | 更新时间: 2026-06-10*
